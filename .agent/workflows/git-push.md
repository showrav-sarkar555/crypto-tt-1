---
description: How to commit and push changes to the GitHub repository
---
// turbo-all

1. Stage all changes:
```
git add -A
```

2. Commit with a descriptive message:
```
git commit -m "<descriptive message>"
```

3. Push to the remote repository:
```
git push origin main
```
